<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wellnest"
        tools:targetApi="31">

        <activity android:name=".reclamation.AjouterReclamation"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.user.testActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.ResetPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.EmailConfirmationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        <activity android:name=".Home"-->
<!--            android:exported="true">-->
<!--        </activity>-->

        <activity android:name=".ui.Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>